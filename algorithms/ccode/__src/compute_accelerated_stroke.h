#ifndef COMPUTE_ACCELERATED_STROKE_H
#define COMPUTE_ACCELERATED_STROKE_H

// Always include Python.h in the very first line in all header files.
#include "Python.h"

PyObject* compute_accelerated_stroke(PyObject *self, PyObject *args);

#endif
